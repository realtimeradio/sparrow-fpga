Copy the files in `zynq-boot-partition` to the boot partition of a Sparrow's SD card in order to use 1GbE control over SFP port 0.

After copying, rename `top.bit.<your FPGA model number>` to `top.bit`
